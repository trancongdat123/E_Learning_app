import 'package:flutter/material.dart';

class ChatAIScreen extends StatelessWidget {

  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Chat with AI')),
      body: Center(
        child: Text('Function about AI chatting will be sticked here.'),),
      );
  }
}