import 'package:flutter/material.dart';

class LessonScreen extends StatelessWidget{

  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(title: Text('Lesson')),
      body: Center(child: Text('The content is now available here.'),),
    );
  }
}